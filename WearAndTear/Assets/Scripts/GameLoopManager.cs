using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameLoopManager : MonoBehaviour
{
    /*private static Queue<int> enemyIDSummons;

    public bool end;
    // Start is called before the first frame update
    void Start()
    {
        enemyIDSummons = new Queue<int>();
        Summoner.Init();
    }

    IEnumerator GameLoop()
    {
        while (end == false)
        {
            if (enemyIDSummons.Count > 0)
            {
                for (int i = 0; i < enemyIDSummons.Count; i++)
                {
                    Summoner.SummonEnemy(enemyIDSummons.Dequeue());
                }
            }
            yield return null;
        }
    }*/

}
